[project]
name = "ai-engineer-intelligence-pipeline"
version = "0.1.0"
description = "AI/venture ecosystem intelligence graph ingestion pipeline"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
